<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/main.css">
    <title>Pixeltify</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

</head>
<body>

  <div class="container">
        <!-- pixel image -->
        <img class="img" src="<%= img %>" alt="pixelated image">

        <!-- ONE FORM 
         
        if the user submits  the search bar, then that will be processed appropriately
        if the user clicks one of the buttons, then that will be processed appropriatetely
        -->
        <form class="form" method="post" action="/search" autocomplete="off">
          <input class="search" type="text" name="searchInput" maxlength="100" placeholder="enter an album,artist or song">
        </form>
        
        <div class="inner-container">
          <% if (locals.albums) { %>
            <% albums.forEach(album=>{ %>
             <form class="inner-form" method="post" action="/check">
               <input type="hidden" name="searchRes" value="<%= album.images[0].url %>">
               <input class="inner-search" type="submit" value="<%= album.name %> by <%= album.artists[0].name %>">
             </form>
            <%}) %>
           <% } else { %>
              <div class="placeholder"></div>
            <% } %>
           <% if (locals.error) { %>
            <p><%= error %></p>
           <% } %>
        </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

</body>
</html>